#include<stdio.h>
int main()
{
    int a,count=0,digit=0;
    scanf("%d",&a);
    while(a>0)
    {
        digit=a%100;
        a/=10;
        if(digit==16||digit==25||digit==36||digit==49||digit==64||digit==81)
        {
            count++;
        }
    }
    printf("%d",count);
}