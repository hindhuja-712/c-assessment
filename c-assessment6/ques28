#include<stdio.h>
int main()
{
    int a,b,max=0,lcm=0,i;
    scanf("%d %d",&a,&b);
    if(a>b)max=a;
    else max=b;
    i=max;
    while(i<=a*b)
    {
        if(i%a==0&&i%b==0)
        {
            lcm=i;
            break;
        }
       i++;
    }
    printf("%d",lcm);
}