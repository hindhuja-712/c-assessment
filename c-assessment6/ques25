#include<stdio.h>
int main()
{
    int a,count=0,digit=0;
    scanf("%d",&a);
    while(a>0)
    {
        digit=a%10;
        a/=10;
        if(digit==2||digit==3||digit==5||digit==7)
        {
            count++;
        }
    }
    printf("%d",count);
}