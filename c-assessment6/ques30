#include<stdio.h>
int main()
{
    int a,b,min=0,hcf=1,i;
    scanf("%d %d",&a,&b);
    if(a>b)min=a;
    else min=b;
    i=1;
    while(i<=a&&i<=b)
    {
        if(a%i==0&&b%i==0)
        {
            hcf=i;
           
        }
       i++;
    }
    printf("%d",hcf);
}