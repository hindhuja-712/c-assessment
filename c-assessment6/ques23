#include<stdio.h>
int main()
{
    int a,count=0,digit=0;
    scanf("%d",&a);
    while(a>0)
    {
        digit=a%10;
        a/=10;
        if(digit==0||digit==1||digit==4||digit==9)
        {
            count++;
        }
    }
    printf("%d",count);
}