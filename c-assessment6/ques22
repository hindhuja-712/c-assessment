#include<stdio.h>
int main()
{
    int a,count=0,digit=0;
    scanf("%d",&a);
    while(a>0)
    {
        digit=a%100;
        a/=10;
        if(digit%2!=0&&digit>9)
        {
            count++;
        }
    }
    printf("%d",count);
}