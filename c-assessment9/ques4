#include<stdio.h>
int main()
{
    int num[5];
    for(int i=0;i<5;i++)
    {
        scanf("%d",&num[i]);
    }
    int max;
    max=num[0];
    for(int i=0;i<5;i++)
    {
        if(max<num[i])
        {
            max=num[i];
        }
    }
    printf("%d",max);
}