#include<stdio.h>
int main()
{
    int a,flag=0,sum=0,copy,las=0;
    scanf("%d",&a);
    copy=a;
    las=copy%100;
    for(int i=2;i<las;i++)
    {
        flag=0;
        if(a%i==0)
        {
            flag=1;
            printf("Not prime");
            break;
        }
    }
    if(flag==0)
    {
        printf("Prime");
    }
    
}