#include<stdio.h>
int main()
{
    int a,b,min=0,hcf=1;
    scanf("%d %d",&a,&b);
    if(a<b)min=a;
    else min=b;
    for(int i=1;i<=a&&i<=b;i++)
    {
        if(a%i==0&&b%i==0)
        {
            hcf=i;
        }
    }
    printf("%d",hcf);
}