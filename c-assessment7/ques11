#include<stdio.h>
int copy,digit=0,rev=0;
void c(int a)
{
    digit=0;
    while(a>0)
    {
        digit++;
        a/=10;
    }
    printf("%d",digit);
}
int main()
{
    int a;
    scanf("%d",&a);
    c(a);
    return 0;
}