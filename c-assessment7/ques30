#include<stdio.h>
int hcf=0,min=0;
void f(int a ,int b)
{
    if(a>b)min=a;
    else min=b;
    for(int i=1;i<=a&&i<=b;i++)
    {
     if(a%i==0&&b%i==0)
     {
         hcf=i;
     }
    }
    printf("%d",hcf);
}
int main()
{
    int a,b;
    scanf("%d %d",&a,&b);
    f(a,b);
    return 0;
}
