#include<stdio.h>
int copy,sum=0;
void c(int a)
{
    while(a>0)
    {
        sum=sum*10+a%10;
        a/=10;
    }
    printf("%d",sum);
}
int main()
{
    int a;
    scanf("%d",&a);
    c(a);
    return 0;
}