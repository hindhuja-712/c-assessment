#include<stdio.h>
int main()
{
    int a;
    scanf("%d",&a);
    int sum=0;
    int copy=0;
    loop:
    if(a>0)
    {
        copy=a%10;
        a/=10;
        sum=sum+copy;
        goto loop;
    }
    printf("%d",sum);
}